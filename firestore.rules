rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write for all paths if the user is authenticated.
    // This is a simple rule for development and should be secured
    // further for a production environment.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
